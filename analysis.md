# Анализ проекта Arcadia-CC

## 🛠 Технологический стек
- **Frontend**: React (v18.2), Three.js (v0.162), Framer Motion, Sass.
- **Backend**: Node.js, Express, MongoDB (Atlas), Airtable API, Nodemailer.
- **Сборка**: react-scripts (Create React App).
- **Стилизация**: SCSS с использованием методологии **BEM**.

---

## 📂 Обзор структуры каталогов

### Корень проекта
- `client/`: Исходный код фронтенда.
- `server/`: Исходный код бэкенда.
- `package.json`: Скрипты для запуска и деплоя всего проекта.

### Клиентская часть (`client/src`)
- `components/`: Основные React-компоненты.
    - `Hero.jsx`: Содержит интеграцию с Three.js.
    - `Kit3D.jsx`: Точка входа для Three.js сцены.
- `pages/`: Страницы приложения (About, ForKids, Home, NotFound).
- `scss/`: Глобальные стили и стили компонентов.
    - Использование BEM (Block, Element, Modifier) прослеживается в именовании классов (например, `.button--outline`, `.section__title`).
- `assets/` & `fonts/`: Статические ресурсы.

### Серверная часть (`server/`)
- `server.js`: Основной файл сервера Express.
- `lib/`: Утилиты, конфигурации и middleware (сессии, авторизация).
- `api/`: Взаимодействие с внешними API (Airtable).
- `mailer/`: Конфигурация Nodemailer для отправки уведомлений.
- `data-access/`: (Планируется) Слой работы с данными.

---

## 🔍 Ключевые наблюдения

### 1. Three.js Интеграция
- В `Kit3D.jsx` используется `FBXLoader` для загрузки 3D-модели (`Pixel.fbx`).
- Имеется прямое обращение к DOM через `document.querySelector('#canvas')`, что не рекомендуется в React. Лучше использовать `ref`.
- Код содержит закомментированные фрагменты и неиспользуемые переменные, что требует очистки.

### 2. SCSS и BEM
- Методология BEM реализована качественно. Селекторы структурированы логично через вложенность Sass (`&__element`, `&--modifier`).
- Используются переменные для цветов и анимаций, что облегчает поддержку.

### 3. Бэкенд и Безопасность
- **⚠️ Внимание**: В `server.js` (строка 216) жестко прописана строка подключения к MongoDB с паролем. Это критическая уязвимость. Рекомендуется перенести в `.env` файл.
- Используются `express-session` и `cookie-parser` для отслеживания сессий пользователей.

### 4. Взаимодействие с API
- Проект активно использует **Airtable** как вторичную базу данных/админку для хранения заявок и заказов.
- Реализована отправка email-уведомлений через Nodemailer.

---

## 💡 Рекомендации
1.  **Безопасность**: Срочно перенести конфиденциальные данные (MongoDB URL, пароли почты, API ключи) в переменные окружения (`.env`).
2.  **Рефакторинг Three.js**:
    - Переписать `Kit3D.jsx` с полным использованием React Refs.
    - Рассмотреть использование `@react-three/fiber` для более декларативного управления 3D-сценой в React.
3.  **Оптимизация**:
    - Удалить закомментированный код в `server.js` и `Kit3D.jsx`.
    - Настроить линтинг (ESLint) для поддержания чистоты кода.
4.  **Развитие**: Реализовать запланированные функции из To-Do списка в `server.js` (корзина, оплата, телеграм-бот).
